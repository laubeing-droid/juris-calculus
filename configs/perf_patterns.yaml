schema_version: "3.0"
fixture_kind: synthetic_official_pack
measurement_scope: audited_application
budgets:
  cold_start_sec: 2.0
  warm_run_sec: 1.0
  branch_run_sec: 2.0
  peak_memory_bytes: 67108864
  audit_event_count: 100
  audit_bundle_bytes: 1048576
notes:
  - Budgets are regression ceilings for the fixed synthetic fixture, not throughput claims for the 21144-rule candidate corpus.
  - Performance must not omit audit events, source checks, or certificate checks.
